# package directories
node_modules
jspm_packages

# build
/build

# next.js
.next

# misc
.DS_Store
.env.keys
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env*.local

# sentry (auth token written by the build plugin)
.env.sentry-build-plugin
.sentryclirc

# local databases
local.db
src/db/*.sql
src/db/*.sql-shm
src/db/*.sql-wal

# build artifacts
tsconfig.tsbuildinfo
bun.lock

# yarn (install cache only; .yarn/releases is intentionally tracked)
.yarn/install-state.gz

# tooling
.mcp.json
.vercel
